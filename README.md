# Openspec UI

VSCode extension for browsing existing Openspec markdown docs.

## Scripts

- `npm run compile`
- `npm run test`
- `npm run watch`

## Releases

- Push a tag like `v0.1.0` to build the VSIX and create a GitHub Release.
